project:
  id: example-project
  species: mouse
  tissue: example_tissue
input:
  object: data/seurat_object.qs
  format: seurat_qs
metadata:
  sample: sample_id
  condition: condition
  batch: batch
  cell_type: cell_type
comparisons:
  - id: group-b-vs-group-a
    factor: condition
    numerator: group_b
    denominator: group_a
