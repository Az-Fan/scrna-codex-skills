# Subset checkpoints

Record the source object checksum/path, annotation field, exact inclusion labels, retained cells by sample, exported raw-count dimensions, and whether the parent object was modified. The exporter does not establish an uncorrected baseline, compare integration or resolutions, compute markers, or finalize annotations; record those in their downstream skill manifests when requested.
