# QC review outputs

Produce per-sample cell counts and quantiles for each QC metric, distribution plots split by sample, filtering waterfall tables, retained/excluded counts by condition and batch, and a machine-readable decision table. Record thresholds and reasons. Preserve pre-filter cell identifiers so every exclusion is reversible and auditable.

