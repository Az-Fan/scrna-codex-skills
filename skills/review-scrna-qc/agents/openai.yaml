interface:
  display_name: "Review scRNA QC"
  short_description: "Auto-detect metrics and build a full QC atlas"
  default_prompt: "Use $review-scrna-qc to auto-detect calculated QC metrics and generate a comprehensive QC atlas and auditable threshold tables without filtering cells."
