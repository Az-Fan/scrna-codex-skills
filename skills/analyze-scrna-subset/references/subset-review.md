# Subset checkpoints

Record source object checksum/path, inclusion rule, retained cells by sample, uncorrected baseline, integration sensitivity, resolution sensitivity, marker evidence, exclusion scenarios, final annotation status, and whether labels were written back to the parent object.

