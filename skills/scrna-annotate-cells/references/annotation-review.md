# Annotation review table

Use columns: `cluster`, `candidate_broad`, `candidate_fine`, `positive_markers`, `negative_markers`, `reference_label`, `sample_bias`, `qc_flag`, `confidence`, `decision`, and `notes`. Require a confirmed `decision` before overwriting or creating the formal annotation used downstream.

