# Benchmark criteria

Report metrics in two groups: batch removal and biological conservation. Include a no-correction baseline. Supplement aggregate scores with per-cell-type and per-sample diagnostics. A recommendation must state whether it is intended for visualization/clustering or statistical testing; do not use integrated values as raw pseudobulk counts.

